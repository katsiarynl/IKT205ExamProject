import { StyleSheet } from "react-native";

const stripestyle = StyleSheet.create({
  screen_webviewstyle: {
    width: "100%",
    height: "100%",
    paddingTop: "8%",
    backgroundColor: "white",
  },
  webviewstyle: {
    padding: 30,
  },
});

export default stripestyle;
