import { StyleSheet } from "react-native";

export const activeStyle = StyleSheet.create({
  constiner: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    flexDirection: "row",
    marginTop: 12,
  },
  titleText: {
    fontSize: 19,
    color: "#2C175C",
  },
});
